//-------------------
//Calculate the sum of the two integers. If the sum
//is in the range 50 to 80, print 65, otherwise print 80.

/*
let sumInt = int1 + Int2;

console.log(sumInt);
if (sumInt >= 50 && sumInt < 80) {
  console.log(65);
} else {
  console.log(80);
}
*/

//Check whether either one of two integers is 15,
//or if their sum is 15. If one of these is the case, print true.
/*
let int7 = 5;
let int8 = 10;
let intSum = int7 + int8;
if (int7 == 15 || int8 == 15 || intSum == 15) {
  console.log(true);
}
//result : true
*/

//------

//77 is the smaller number between o and 100 which multiply by 7 and 11 at same time...
/*
let number1 = 1;
let number2 = 7;
if (
  number1 % 7 == 0 ||
  number2 % 7 == 0 ||
  number1 % 11 == 0 ||
  number2 % 11 == 0
) {
  console.log(true);
}
//result : true
*/

///Calculate the sum of the two given integers. If the two
// values are the same, then print triple their sum.
/*

let int22 = 4;
let int21 = 3;
let numTrip = (int22 + int21) * 3;
if (int22 === int21) {
  console.log(numTrip);
}
*/
