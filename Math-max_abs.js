//Mathematical expressions

//Declare the variables a, b and c, and give them number values. Check which one out of the three variables has the largest value and
// print it to the console. Then change the values of the variables to see if your conditional still works.
/*
let a, b, c;
a = 10;
b = 44;
c = 22;


console.log(Math.max(a, b, c));
//result : 44

*/
//--------------Math.max---------------
/*
let x, y, z; //
x = 2; //
y = 5;
z = x + y;
console.log(z); //addition of x + y is equal to 7 so 7 will be the value of Z
console.log(Math.max(33, 44, 52, 111111, 76, 6, z)); //the maximal numer
console.log(Math.min(33, 44, 5, 6, 0.3, 7, 7111, y)); //the minimal numer

*/

/*
//Declare the variables a, b and c, and give them number values.
//Check which one out of the three variables has the largest value
//and print it to the console. Then change the values of the variables
//to see if your conditional still works.
/*
let x, y, h; //
x = 2; //
y = 5;
h = 25;
console.log(Math.max(x, y, h)); //the maximal numer
------*/
//
//
/*----------sumTriple-----------*/
/*----------sumTriple-----------*/

//Calculate the sum of the two given integers
//Write a JavaScript program to compute the sum of the
//two given integers. If the two values are same, then returns triple their sum.
/*
function sumTriple(x, y) {
  if (x == y) {
    return 3 * (x + y);
  } else {
    return x + y;
  }
}
console.log(sumTriple(10, 20));
console.log(sumTriple(10, 10));

//RESULT _: 30 , 60


/*----------sumTriple-----------*/
/*----------sumTriple-----------*/

/*
function sumTriple(x, y) {
  if (x == y) {
    return 3 * (x + y);
  } else {
    return x + y;
  }
}
console.log(sumTriple(10, 20));
console.log(sumTriple(1, 1));

//RESULT _: 30 , 6

*/
//

//

//

/*------DISTANCE BETWEEN NUMBERS------Math.abs----------

function difference(a, b) {
  return Math.abs(a - b);
}

console.log(difference(3, 5));
// expected output: 2 , 2 is the distance of 3 to reach 5

console.log(difference(5, 3));
// expected output: 2

console.log(difference(1.23456, 7.89012));
// expected output: 6.6555599999999995   6 is the distance of 1 to reach 7


--------------------------


/*------------------------------------------------------------
//----------------------EXERCISES

*/
// Check whether the sum of two integers is 8, or
// whether their difference is 8. If one of these is the case, print true.
let int5 = 3;
let int6 = 5;
let numDiff = Math.abs(int5 - int6);
//3+5=2
let numSum = int5 + int6;
console.log(numDiff);
//
if (numSum == 8 || numDiff == 8) {
//CHECK  whether their difference is 8. 
//If one of these is the case, print true.
  console.log(true);
}
//result
/*
2
true
*/







--------------------------------------------------------------*/
